# my_diary

## What
my_diary는 개인 개발 과정을 기록하고,
DEVLOG를 기반으로 핵심 내용을 자동 요약·정리하는 일기형 프로젝트입니다.

## Why
개발 과정의 모든 기록(DEVLOG)은 보존하되,
외부에 공개할 정보는 선별된 요약본만 유지하기 위해
DEVLOG와 README를 분리된 구조로 설계했습니다.

## How
- 모든 작업 기록은 DEVLOG/YYYY-MM-DD 형식으로 누적 저장합니다.
- 최신 DEVLOG를 가리키는 DEVLOG_LAST.md를 자동 갱신합니다.
- DEVLOG_LAST.md를 기준으로 README 정리에 필요한 핵심 재료를 추출합니다.

## Core Feature
- DEVLOG_LAST를 자동으로 최신 DEVLOG로 갱신하는 스크립트
- DEVLOG를 기반으로 README를 정리하는 자동화 구조

## Rules
- DEVLOG는 전체 기록용이며, 삭제하지 않습니다.
- README.md에는 구조·규칙·재사용 가능한 설계만 기록합니다.
- 날짜, 감정, 시행착오는 README에 포함하지 않습니다.
## Completion Criteria (Fixed)

이 일기앱은 다음 조건을 충족하면 ‘완성’으로 간주한다.

1. 사용자는 하루에 하나 이상의 일기 항목을 기록할 수 있다.
2. 일기 항목은 날짜 기준으로 저장되고 다시 조회할 수 있다.
3. 항목 구조(템플릿)는 코드 레벨에서 고정하며,
   사용자는 내용만 입력한다.
4. 인증·저장·조회 흐름은 안정적으로 동작하면 충분하다.
5. UI는 미려함보다 가독성과 입력 안정성을 우선한다.

다음 항목들은 의도적으로 범위에서 제외한다.
- 통계·차트·자동 분석
- 외부 공유 기능
- 고급 편집 UI

본 기준을 충족하면 추가 기능 없이 프로젝트를 종료한다.
## v1.0 Release Checklist

- [ ] 날짜 기준으로 일기 생성 가능
- [ ] 일기 조회/수정/삭제 정상
- [ ] 템플릿 기반 항목 입력 정상
- [ ] 로그인/저장/조회 안정
- [ ] 빈 상태/에러 메시지 최소 처리
- [ ] 모바일 기본 가독성 확보

> 위 항목을 모두 충족하면 v1.0은 출시로 간주한다.
이 기준은 개발 중 변경하지 않으며, v1.0 출시 판단의 유일한 기준으로 사용한다.
## Note on Statistics

통계 분석은 본 프로젝트의 가장 중요한 차별점이다.
다만 v1.0에서는 의도적으로 제외하고,
v1.1에서 핵심 기능으로 구현한다.

